[pytest]
addopts = -ra -q
testpaths = tests
python_files = test_*.py
markers =
    smoke: quick health checks for core endpoints
    contract: schema and response-shape validation tests
filterwarnings =
    ignore::DeprecationWarning

